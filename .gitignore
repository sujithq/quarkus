target/
db-quarkus/
*.log

# Agentic CodeQL pipeline scratch / outputs (regenerated by detect/propose/verify)
.aw-verify/
results/
*.sarif

# Agentic CodeQL pipeline deliverables (produced locally; committed only on phase branches in CI)
.codeql/models/generated-sql-injection-sinks.yaml
docs/codeql-gap-analysis.md
